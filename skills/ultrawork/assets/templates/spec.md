# 功能规格（Spec）—— WHAT / WHY（intent = 单一信源）

> 只写"做什么 / 为什么"，不写"怎么实现"（实现在 plan.md）。intent 是源，代码由它派生。
> 每条需求必须**可测**：含边界、输入 / 输出、验收标准。含糊处先过 Clarify（references/clarify-taxonomy.md 的 10 类扫描）再定稿。
> spec 定稿后进 design → plan → tasks → analyze；**最终连同 design.md + plan.md + tasks + analyze 摘要一起过人工闸①**（用户逐 MF 过支线表）。接口级 IO 以 **design §2** 为权威，spec 的 FR 只表语义字段 + 验收意图。

## 目标（一句话）

## 背景 / 为什么要做

## 非目标（明确不做的范围）
-

## 功能需求（逐条，每条可测）

### FR-1 <名称>
- 描述：
- 输入：类型 / 范围 / 必填性 / 校验规则
- 输出：数据结构 / 状态码 / 错误形态
- 边界与异常：空 / 超限 / 并发 / 越权 / 幂等 / 超时 / 部分失败
- 验收标准（Given–When–Then，可转成断言）：

<!-- 复制 FR 块继续 FR-2, FR-3 … -->

## 非功能需求
- 性能：P95 / 吞吐 / 并发目标
- 安全：鉴权 / 授权 / 审计 / 数据保护
- 兼容 / 迁移：向后兼容要求、数据迁移策略

## 术语表（消歧，防同词不同义）
| 术语 | 定义 |
|---|---|
